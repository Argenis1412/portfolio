/**
 * @deprecated — Import directly from 'src/api/' subdirectory.
 * This file is a compatibility shim and will be removed once all
 * consumers have been migrated to the new layered structure.
 *
 * Preferred imports:
 *   import { fetchAbout } from './api/portfolioService';
 *   import { postChaosDrain } from './api/chaosService';
 *   import { ApiError } from './api/client';
 */
export * from './api/index';
